.PHONY: clean All

All:
	@echo "----------Building project:[ WordTest - Debug ]----------"
	@cd "text-query-system" && "$(MAKE)" -f  "WordTest.mk"
clean:
	@echo "----------Cleaning project:[ WordTest - Debug ]----------"
	@cd "text-query-system" && "$(MAKE)" -f  "WordTest.mk" clean
